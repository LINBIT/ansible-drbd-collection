# SPDX-License-Identifier: MIT
---
- name: Remove version lock for DRBD kernel package (DNF)
  community.general.dnf_versionlock:
    name: "{{ _drbd_module_package }}"
    state: absent
  changed_when: false
  failed_when: false
