# SPDX-License-Identifier: MIT
---
- name: Open UFW ports for DRBD
  community.general.ufw:
    rule: allow
    port: "{{ item.port | regex_replace('-', ':') }}"
    proto: "{{ item.proto }}"
  loop: "{{ drbd_install_firewall_ports }}"
